LoRa Signpost Gateway
=====================

This is an application specific receiver for LoRa packets from a signpost.

It takes the packets, parses them, and publishes the data streams
for consumption by the other gateway services
